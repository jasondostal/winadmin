# Binaries
*.exe
*.dll
/bin/

# Go
/vendor/
*.test
*.out

# OS
.DS_Store
Thumbs.db

# Source material kept locally for reference, not part of the project
*.wbt

# local build artifact: the `fleet` binary collides with the fleet/ package dir,
# so ignore the file but keep the directory tracked.
/fleet
!/fleet/
